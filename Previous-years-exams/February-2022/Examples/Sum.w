program Sum(n,s);

// a simple program to compute the sum of the first n natural numbers

n:= 10;
s:= 0;
for (i:= 1; i <= n; i:= i + 1) do
   for (j:= 1; j <= i; j:= j + 1) do
      s:= s + 1

// Final State: ["n->10","s->55"]
